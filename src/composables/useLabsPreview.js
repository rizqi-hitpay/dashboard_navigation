import { ref } from 'vue'

// Preview switcher state for HitPay Labs (Figma: 1:17127) — when the catalog
// only has 2 apps, the directory shows just the featured cards and the
// sidebar drops the category items.
export const labsFewApps = ref(false)
